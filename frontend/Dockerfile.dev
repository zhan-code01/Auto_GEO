# AutoGeo Frontend development image

FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --include=dev

EXPOSE 5173

CMD ["npm", "run", "vite:dev", "--", "--host", "0.0.0.0", "--port", "5173"]
