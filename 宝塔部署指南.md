# AutoGeo 宝塔服务器部署指南（依据当前代码重写）

> 适用代码状态：分支 `article_dev`，后端 FastAPI + Playwright，前端 Vue3（网页版 + 可选的 Electron 桌面版），数据库 **PostgreSQL 强制**。
> 本文档是依据项目里 `deploy/production/docker-compose.yml`、`backend/Dockerfile`、`frontend/Dockerfile`、`backend/entrypoint.sh`、`backend/config.py` 的实际内容整理的，**不依赖任何旧说明**。
>
> 文中 `<服务器公网IP>` 替换为你的真实 IP（如果你沿用之前那台 `8.163.39.114`，它就是那台）。

---

## 〇、先纠正 3 个常见误区（最重要）

你之前的理解里有几处和现在代码对不上，先说清楚，后面操作才不会走偏：

| 你的理解 | 实际情况（当前代码） |
|---|---|
| "把 exe 部署到宝塔服务器" | ❌ exe 是 **Electron 桌面客户端**（`frontend` 里 `npm run dist:win` 打的包），它跑在**你自己的 Windows 电脑**上，当作"遥控器"在本地浏览器里做各平台登录/发文。**服务器上完全不需要它。** |
| "需要先打包好数据库上传" | ❌ 数据库是 **PostgreSQL**，由 `postgres` 容器启动后，**自动建库 + 自动跑 Alembic 迁移建表**（`backend/entrypoint.sh` 里 `alembic upgrade head`）。数据存在 Docker 卷里持久化。**你什么都不用上传，只需在 `.env` 里设一个强密码。** |
| "前后端部署到服务器" | ✅ 对。服务器上跑 **3 个 Docker 容器**：`postgres`（库）、`backend`（后端 API + 浏览器自动化）、`frontend`（nginx 托管的网页前台）。你用**浏览器**访问网页前台即可使用整套系统。 |

**一句话总结部署目标**：在宝塔服务器上用 Docker 把"数据库 + 后端 + 网页前端"三个容器跑起来，外网通过宝塔反向代理用浏览器访问。exe 桌面端是可选的、留在你电脑上的补充。

---

## 一、部署前置清单

### 必须准备
| 项目 | 说明 / 怎么确认 |
|---|---|
| 宝塔面板 + Docker | 面板里「软件商店」装 **Docker 管理器**。 |
| 一台 Linux 服务器 | 建议 **≥4 核 8G 内存**（Chromium 浏览器自动化比较吃内存）。 |
| DeepSeek API Key | 形如 `sk-xxxx`，用于 AI 写文章。没有它系统能起来，但生成文章会失败。 |
| 服务器公网 IP | 能 SSH / 宝塔终端登录。 |
| 本地源码（当前工作区） | 就是你电脑上的 `D:\GEO\Auto_GEO-main`，包含未提交的新代码（`agent_v2/`、`迁移0033` 等），**必须用这份，不要去 git 上 clone 旧版**。 |

### 可选（不是必须先有）
| 项目 | 作用 |
|---|---|
| RAGFlow | 知识库问答。`.env` 里 `RAGFLOW_API_KEY` / `RAGFLOW_BASE_URL` 留空也能跑，只是用不了知识库。 |
| n8n | 自动化工作流。生产 compose **未包含** n8n，基础发文/检测不依赖它。 |
| 飞书机器人 | 通知用，可选。 |

### 不需要做的事
- ❌ 不用打包 / 上传数据库文件。
- ❌ 不用把 exe 传到服务器。
- ❌ 不用 `./deploy.sh`：它**强制要求 git 仓库**且写死了旧 master 提交号，你现在本地是 `article_dev` 且有未提交改动，用它必报错。本指南改为**直接跑 `docker compose` 命令**。

---

## 二、整体流程（8 个阶段，首次约 60–120 分钟，慢在首次构建拉镜像）

| 阶段 | 做什么 | 在哪做 |
|---|---|---|
| 1 | 宝塔装 Docker + 配镜像加速器（中国服务器必做） | 宝塔面板 |
| 2 | 把本地源码打成 tar 包（排除 node_modules/.git） | 你电脑 |
| 3 | 上传 tar 到服务器 `/opt` 并解压 | 宝塔「文件」+「终端」 |
| 4 | 配置 `.env`（填密码 / 密钥 / DeepSeek） | 服务器终端 |
| 5 | 构建镜像并启动 3 个容器（最慢） | 服务器终端 |
| 6 | 宝塔建反向代理（80 → 127.0.0.1:8080，勾 WebSocket） | 宝塔面板 |
| 7 | 浏览器验证（看到登录页 = 成功） | 你电脑浏览器 |
| 8 | 安全收尾（防火墙 / 改面板密码） | 宝塔面板 |

---

## 三、阶段 1：宝塔装 Docker + 镜像加速器

1. 宝塔左侧 **软件商店** → 搜 `Docker` → 装 **Docker 管理器**，等装完（左侧出现「Docker」入口）。
2. **中国服务器必做**：左侧 **Docker** → **设置 / 镜像加速**，填入一个可用加速器，例如：
   - `https://docker.m.daocloud.io`
   - 或阿里云容器镜像加速器（控制台免费申请，格式 `https://xxxx.mirror.aliyuncs.com`）
   
   保存后**重启 Docker**（面板里一般有重启按钮，或终端 `systemctl restart docker`）。
   > 不配加速器的话，首次构建拉 `python:3.12-slim`、`node:20-alpine`、`nginx:alpine`、`postgres:15-alpine` 会极慢甚至超时。

验证（终端）：
```bash
docker --version
docker compose version
```
两条都打印版本号即成功。

---

## 四、阶段 2：把本地源码打包（在你电脑上，Git Bash / 终端执行）

关键点：**排除 `frontend/node_modules`**（它巨大且镜像构建时会 `npm ci` 重新装）和 `.git`（构建不需要，且避免 git 依赖）。项目里已有 `.dockerignore` 会在构建时再次兜底排除。

```bash
cd /d/GEO
tar --exclude='Auto_GEO-main/frontend/node_modules' \
    --exclude='Auto_GEO-main/.git' \
    --exclude='Auto_GEO-main/backend/__pycache__' \
    --exclude='Auto_GEO-main/network-cache' \
    -czf autogeo-src.tar.gz Auto_GEO-main
```
生成的 `D:\GEO\autogeo-src.tar.gz` 就是你要上传的包（通常几百 MB，主要是前端源码文本，压缩后不大）。

> 当前工作区我已经帮你打包好了：`D:\GEO\autogeo-src.tar.gz`（约 62 MB），你直接跳到阶段 3 上传即可。  
> 如果你以后自己重新打包：用 7-Zip 把 `Auto_GEO-main` 压成 `.tar.gz`，**压缩前手动去掉 `frontend/node_modules` 和 `.git` 两个文件夹**。

---

## 五、阶段 3：上传并解压到服务器

1. 宝塔左侧 **文件** → 进 `/opt`（没有就新建目录）。
2. 右上角 **上传** → 选你电脑上的 `autogeo-src.tar.gz` → 等 100%。
3. 宝塔左侧 **终端**，逐行执行：
```bash
cd /opt
tar xzf autogeo-src.tar.gz
ls /opt/Auto_GEO-main/deploy/production/
```
看到 `docker-compose.yml`、`.env.example`、`deploy.sh` 等 = 解压成功。
> 目录结构必须保持 `Auto_GEO-main/deploy/production/...`，因为 compose 里构建上下文是 `../../backend`、`../../frontend`（相对路径）。

---

## 六、阶段 4：配置 `.env`（最关键，密钥都在这里）

如果是**全新服务器**（之前没部署过）：
```bash
cd /opt/Auto_GEO-main/deploy/production
cp .env.example .env
```
> 如果这台服务器**之前已经部署过**（代码可能已在 `/opt/Auto_GEO-main`，且 `deploy/production/.env` 已存在），**不要覆盖**，直接 `nano .env` 复查旧值即可。

生成两个随机密钥（各跑一次，把输出分别复制备用）：
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

编辑 `.env`：
```bash
nano .env
```
按下面改（其余保持默认）：
```ini
# 1) 数据库（PostgreSQL，强制。密码只用字母/数字/下划线，避免 URL 转义问题）
POSTGRES_DB=autogeo
POSTGRES_USER=autogeo
POSTGRES_PASSWORD=这里换成你的强密码_如AutoGeo2026_db
DATABASE_URL=postgresql://autogeo:这里换成你的强密码_如AutoGeo2026_db@postgres:5432/autogeo

# 2) 两个本地密钥（分别粘贴上面生成的两条）
AUTO_GEO_ENCRYPTION_KEY=密钥1
JWT_SECRET_KEY=密钥2

# 3) DeepSeek（AI 写文章必需）
DEEPSEEK_API_KEY=sk-你的deepseek密钥

# 4) 可选：知识库 / 自动化（没有就留空）
RAGFLOW_API_KEY=
RAGFLOW_BASE_URL=

# 5) 绑定的端口（默认已是 127.0.0.1，保持不变——只对本机开放，更安全）
FRONTEND_HTTP_BIND=127.0.0.1:8080
BACKEND_HTTP_BIND=127.0.0.1:8001

# 6) 如果你以后要用桌面 exe 直连服务器，把下面改成你的服务器IP（网页访问不需要）
# CORS_ORIGINS=http://<服务器公网IP>
```
> ⚠️ 最容易踩的坑：`POSTGRES_PASSWORD` 或 `DATABASE_URL` 留空/写错 → 后端一启动就以 `Only PostgreSQL DATABASE_URL values are supported` 崩溃。务必填且两者密码一致。

保存退出：`Ctrl+O` → 回车 → `Ctrl+X`。

验证：`grep -E "POSTGRES_PASSWORD|DATABASE_URL|AUTO_GEO_ENCRYPTION_KEY|JWT_SECRET_KEY|DEEPSEEK_API_KEY" .env`，确认都有值。

---

## 七、阶段 5：构建镜像并启动（最慢，耐心等）

> 不要用 `./deploy.sh`（它要 git 且写死旧提交号）。我们直接用 `docker compose` 构建当前源码。

```bash
cd /opt/Auto_GEO-main
docker compose --env-file deploy/production/.env -f deploy/production/docker-compose.yml build
docker compose --env-file deploy/production/.env -f deploy/production/docker-compose.yml up -d
```

这一步在干嘛（首次会很慢，正常现象，别中断）：
1. 拉 4 个基础镜像（postgres / python:3.12-slim / node:20-alpine / nginx:alpine）。
2. 后端镜像里 `pip install` 全部 Python 依赖 + `playwright install chromium`（约 150MB，**国内常卡**，见下方"中国服务器补充说明"）。
3. 前端镜像里 `npm ci` 装依赖 + `vite build` 构建网页（`out/renderer`）。
4. 启动 3 个容器；后端容器启动时**自动 `alembic upgrade head` 建表**。

看到容器表，`autogeo_postgres` / `autogeo_backend` / `autogeo_frontend` 状态为 `Up`（或 `healthy`）即成功。

验证（服务器终端）：
```bash
curl http://127.0.0.1:8080/api/health
```
应返回 JSON，含 `"status": "ok"` 且 `"database": {"status": "connected"}`。

### 中国服务器补充说明（已提前帮你配好）
你当前工作区的两个 Dockerfile 我已经加了国内镜像源，你**不需要再手动改**：
- `frontend/Dockerfile`：`RUN npm config set registry https://registry.npmmirror.com`（在 `npm ci` 前）。
- `backend/Dockerfile`：`ENV PLAYWRIGHT_DOWNLOAD_HOST=https://cdn.npmmirror.com/binaries/playwright`（Playwright 从 npmmirror 下载 Chromium）。
- 另外我也已把打包好的源码包生成在 `D:\GEO\autogeo-src.tar.gz`（62 MB），里面排除了 `node_modules` 和 `.git`，你直接上传即可。

如果你拿到的是一份**全新的、没改过的源码**，才需要自己加这两行；当前这份已经好了。

---

## 八、阶段 6：宝塔建反向代理（外网 80 端口访问）

1. 宝塔左侧 **网站** → **添加站点**：域名填 `<服务器公网IP>`（纯 IP 即可），PHP 版本选 **纯静态**，提交。
2. 站点列表里**点站点名**进入设置 → **反向代理** → **添加反向代理**：
   - 代理名称：`autogeo`
   - 目标 URL：`http://127.0.0.1:8080`
   - 发送域名：`$host`
   - ⚠️ **务必勾选「WebSocket 支持」**（实时日志、智能对话走 `/ws`，不勾会断）。
3. 保存。

验证（终端）：`curl -I http://127.0.0.1/` 应返回 `HTTP/1.1 200`。

---

## 九、阶段 7：浏览器验证（看到界面 = 部署成功）

在你自己电脑浏览器：
1. 打开 `http://<服务器公网IP>/` → 应看到 **AutoGeo 登录页**。
2. 打开 `http://<服务器公网IP>/api/health` → 看到 JSON 健康信息。
3. 注册账号 → 登录 → 进入主界面。

**全链路通的标志**：能登录进主界面（说明 前端 + 后端 + PostgreSQL + WebSocket 都正常）。

---

## 十、阶段 8：安全收尾（别跳过）

1. **防火墙**：宝塔「安全」（以及云厂商安全组）只放行 **80（或 443）、8888、22**。**不要**对公网放行 `8001` / `6080` / `5432`。
2. **改面板密码 + 安全入口**：左侧「面板设置」改面板密码、改安全入口路径为自己随机串（之前的入口路径若已在别处出现过，视为已泄露）。
3. （可选）给站点配 HTTPS：宝塔站点「SSL」申请免费证书并强制 HTTPS，之后反向代理目标改成 `https://127.0.0.1:8080` 也可；本文先用 HTTP 跑通。

---

## 十一、日常运维命令

```bash
cd /opt/Auto_GEO-main
# 看状态
docker compose --env-file deploy/production/.env -f deploy/production/docker-compose.yml ps
# 看后端实时日志（排错首选）
docker compose --env-file deploy/production/.env -f deploy/production/docker-compose.yml logs -f backend
# 重启
docker compose --env-file deploy/production/.env -f deploy/production/docker-compose.yml restart
# 停止 / 启动（不重建）
docker compose --env-file deploy/production/.env -f deploy/production/docker-compose.yml down
docker compose --env-file deploy/production/.env -f deploy/production/docker-compose.yml up -d
```

**更新代码**（你本地改了东西要上服务器）：
1. 在本地重新打 tar 包（阶段 2 命令）。
2. 上传到服务器、解压覆盖 `/opt/Auto_GEO-main`。
3. 重新构建并启动：
   ```bash
   cd /opt/Auto_GEO-main
   docker compose --env-file deploy/production/.env -f deploy/production/docker-compose.yml up -d --build
   ```
   - 前端改动 → 必须 `--build` 重新构建静态文件。
   - 后端**纯源码**改动 → 因为 `docker-compose.yml` 已把 `../../backend` 挂载进容器，直接 `restart backend` 即可，无需重建。
   - 后端依赖 / Dockerfile / 迁移改动 → 必须 `--build`。

---

## 十二、常见问题排查

| 现象 | 原因 / 解决办法 |
|---|---|
| 跑 `./deploy.sh` 报 `git rev-parse HEAD` 失败 | 这是旧脚本的 git 依赖问题。**别用 deploy.sh**，按本指南阶段 5 用 `docker compose` 直接构建。 |
| 拉镜像卡住 / timeout | 没配 Docker 镜像加速器。回阶段 1 配置后重来。 |
| 前端构建卡在 `npm ci` | npm 源慢，按阶段 7 的"中国服务器补充说明"给 frontend Dockerfile 加 npmmirror。 |
| 后端构建卡在 `playwright install chromium` | 按阶段 7 给 backend Dockerfile 加 `PLAYWRIGHT_DOWNLOAD_HOST` 镜像。 |
| 后端起不来，日志含 `Only PostgreSQL DATABASE_URL values are supported` | `.env` 里 `DATABASE_URL` 不是 `postgresql://` 开头，或 `POSTGRES_PASSWORD` 没填。检查阶段 4。 |
| 能开首页但登录/接口 502 | 反向代理没勾 WebSocket，或后端容器没 `healthy`。看 `logs -f backend`。 |
| 数据库想备份 | 数据在 Docker 卷 `autogeo_postgres_data`。备份：`docker compose ... exec -T postgres pg_dump -U autogeo -d autogeo > backup.sql`。 |

---

## 十三、关于"桌面 exe"（可选，非必须）

网页版跑通后你就能用了。如果你**确实想**在自己电脑用桌面 exe 而不是浏览器：
1. 在本地用 `frontend` 的 `npm run dist:win` 打 exe，且构建时把 API 地址指向服务器域名（改 `VITE_API_BASE_URL` 等构建变量）。
2. 在服务器 `.env` 的 `CORS_ORIGINS` 加上你的服务器域名，允许 exe 跨域直连后端。
3. 各平台登录授权如果在服务器上做，可通过后端自带的 noVNC（6080）在浏览器里远程看 Playwright 浏览器扫二维码；需在宝塔再加一条反向代理（目标 `127.0.0.1:6080`，同样勾 WebSocket）。

> 对新手建议：**先只用网页版**，最省事、最稳。
