# AutoGeo UI 设计文档

## 一、设计概述

**设计版本**：v1.0
**设计日期**：2025-01-08
**设计状态**：已完成，待实现
**预览文件**：`fronted/preview.html`

---

## 二、设计风格

### 2.1 视觉风格

| 属性 | 值 |
|-----|-----|
| 主题 | 暗色主题 (Dark Theme) |
| 主色调 | #4A90E2 (蓝色) |
| 成功色 | #4CAF50 (绿色) |
| 警告色 | #FF9800 (橙色) |
| 危险色 | #E53935 (红色) |
| 字体 | Noto Sans SC (中文) + JetBrains Mono (代码) |

### 2.2 设计特点

- **玻璃态效果**：半透明背景 + 背景模糊
- **渐变卡片**：四种主题色渐变
- **悬浮交互**：卡片悬浮抬升效果
- **状态指示器**：带发光效果的状态点
- **平滑动画**：fadeIn、pulse、slideIn

---

## 三、页面结构

### 3.1 整体布局

```
┌─────────────────────────────────────────────────────┐
│                   Sidebar (240px)                   │
│  ┌────────┐  ┌────────────────────────────────────┐ │
│  │ Logo   │  │            Header (60px)           │ │
│  │        │  ├────────────────────────────────────┤ │
│  ├────────┤  │                                    │ │
│  │ 导航   │  │          Main Content              │ │
│  │ 菜单   │  │                                    │ │
│  │        │  │                                    │ │
│  │        │  │                                    │ │
│  ├────────┤  │                                    │ │
│  │ 状态   │  │                                    │ │
│  └────────┘  └────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### 3.2 页面列表

| 页面 | 路由 | 说明 |
|-----|------|------|
| 概览 | Dashboard | 统计数据、快速操作、最近发布 |
| 账号管理 | Accounts | 账号卡片网格、添加/编辑账号 |
| 文章管理 | Articles | 文章列表、编辑/预览文章 |
| 批量发布 | Publish | 分步骤发布流程 |
| 设置 | Settings | 平台配置、通知设置等 |

---

## 四、组件设计

### 4.1 导航栏组件

```html
<nav class="nav-item">
    <span>图标</span>
    <span>菜单文字</span>
</nav>
```

**状态**：
- 默认：灰色文字
- 悬浮：浅蓝背景 + 蓝色左侧边条
- 激活：浅蓝背景 + 蓝色左侧边条 + 白色文字

### 4.2 统计卡片

```html
<div class="gradient-{color} rounded-2xl p-5">
    <div class="text-4xl font-bold">数值</div>
    <div class="text-sm">标题</div>
    <div class="text-xs mt-2">趋势</div>
</div>
```

**渐变类型**：
- `gradient-blue`：蓝色渐变
- `gradient-green`：绿色渐变
- `gradient-orange`：橙色渐变
- `gradient-purple`：紫色渐变

### 4.3 账号卡片

```html
<div class="bg-[#252526] rounded-2xl p-5 card-hover">
    <div class="platform-icon bg-{color}">平台字</div>
    <div class="status-dot status-{state}"></div>
    <h4>账号名称</h4>
    <p>@username</p>
</div>
```

**平台颜色**：
- 知乎：#0084FF (蓝色)
- 百家号：#E53935 (红色)
- 搜狐号：#FF6B00 (橙色)
- 头条号：#333333 (黑色)
- 公众号：#07C160 (绿色)

### 4.4 发布流程步骤

```
┌───┐    ┌───┐    ┌───┐    ┌───┐
│ 1 │────│ 2 │────│ 3 │────│ 4 │
└───┘    └───┘    └───┘    └───┘
激活     激活     禁用     禁用
```

**步骤**：
1. 选择要发布的文章
2. 选择发布账号
3. 确认发布
4. 查看进度

---

## 五、交互设计

### 5.1 页面切换

```javascript
// 导航点击切换页面
navItems.forEach(item => {
    item.addEventListener('click', () => {
        // 更新导航状态
        // 切换页面内容
        // 更新标题
    });
});
```

### 5.2 复选框

```javascript
// 自定义复选框交互
checkbox.addEventListener('click', () => {
    checkbox.classList.toggle('checked');
});
```

### 5.3 动画效果

| 动画 | 用途 | 时长 |
|-----|------|------|
| fadeIn | 页面内容进入 | 0.3s |
| pulse | 加载中状态 | 2s 循环 |
| slideIn | 侧边栏进入 | 0.3s |

---

## 六、响应式设计

### 6.1 断点

| 尺寸 | 说明 |
|-----|------|
| < 768px | 移动端（暂不支持） |
| 768px - 1024px | 平板（侧边栏可折叠） |
| > 1024px | 桌面端（完整布局） |

### 6.2 卡片网格

- 桌面端：3列网格
- 小屏：2列或1列自适应

---

## 七、CSS 变量

```css
:root {
    /* 背景色 */
    --bg-primary: #1E1E1E;
    --bg-secondary: #252526;
    --bg-tertiary: #2D2D30;

    /* 主题色 */
    --primary: #4A90E2;
    --success: #4CAF50;
    --warning: #FF9800;
    --danger: #E53935;

    /* 文字色 */
    --text-primary: #FFFFFF;
    --text-secondary: #AAAAAA;

    /* 边框色 */
    --border: #3E3E42;
}
```

---

## 八、图标系统

| 类型 | 说明 | 示例 |
|-----|------|------|
| Emoji | 导航、操作图标 | 🏠 👥 📝 🚀 |
| Unicode | 状态图标 | ✅ ⚠️ ⏳ 🔄 |
| CSS绘制 | 平台图标 | 带背景色的文字缩写 |

### 平台图标

```
┌─────────┐
│   知    │  知乎 - 蓝色背景
│  #0084FF│
└─────────┘

┌─────────┐
│   百    │  百家号 - 红色背景
│ #E53935 │
└─────────┘
```

---

## 九、实现技术栈

### 9.1 当前实现 (预览)

- **HTML5** - 结构
- **Tailwind CSS CDN** - 样式
- **Vanilla JS** - 交互

### 9.2 生产环境计划

根据 `FRONTEND-ARCHITECTURE.md`：

- **Electron** - 桌面应用框架
- **Vue 3** - 前端框架
- **Element Plus** - UI组件库
- **SCSS** - 样式预处理
- **Pinia** - 状态管理

---

## 十、待实现功能

### 10.1 交互功能

- [ ] 页面切换动画
- [ ] 账号卡片 CRUD 操作
- [ ] 文章列表分页
- [ ] 发布流程步骤验证
- [ ] 实时进度更新

### 10.2 数据对接

- [ ] 连接后端 API
- [ ] WebSocket 实时通信
- [ ] 本地数据持久化
- [ ] Cookies 安全存储

### 10.3 增强功能

- [ ] 深色/浅色主题切换
- [ ] 国际化支持
- [ ] 键盘快捷键
- [ ] 拖拽排序
- [ ] 批量操作

---

## 十一、设计资源

| 资源 | 路径 |
|-----|------|
| UI预览 | `fronted/preview.html` |
| 截图 | `docs/images/ui-preview.png` (待添加) |
| 设计规范 | 本文档 |

---

**文档版本**：v1.0
**最后更新**：2025-01-08
**维护者**：开发者
